source 'https://rubygems.org'

# Specify the Ruby version if needed
# ruby '2.7.0'

# Add the gem itself if it's meant to be a gem
# gem 'blix-opds', path: '.'

# Runtime dependencies
gem 'nokogiri', '~> 1.11'

# Development dependencies
group :development, :test do
  gem 'rspec', '~> 3.10'
  gem 'pry', '~> 0.14'
end

# Test dependencies
group :test do
  #gem 'simplecov', '~> 0.21', require: false
  gem 'timecop'
end

# Add any other gems your project depends on
# For example:
# gem 'activesupport', '~> 6.1'